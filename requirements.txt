# Stage 1 — Ingestion (Kafka + Pydantic contract)
kafka-python==3.0.9
pydantic==2.13.4
loguru==0.7.3

# Document format support — python-docx/pypdf are also reused by the RAG
# loader (Stage 3) to parse .docx/.pdf files; reportlab is only used to
# generate the sample .pdf fixture in scripts/seed_sample_documents.py.
python-docx==1.2.0
pypdf==6.14.2
reportlab==5.0.0

# Stage 2 — Delta Lakehouse (Bronze/Silver/Gold)
# Requires a local JDK 17 + winutils.exe on Windows — see README
# "Lakehouse setup" for the exact provisioning commands.
pyspark==3.5.0
delta-spark==3.2.0

# Stage 3 — RAG pipeline (chunking, embeddings, hybrid search, reranking,
# grounded generation). deltalake+pyarrow read Silver without needing a
# SparkSession — RAG only needs a handful of small text rows.
deltalake==1.6.2
pyarrow==25.0.0
chromadb==1.5.9
sentence-transformers==5.6.0
rank-bm25==0.2.2
torch==2.13.0
transformers==5.14.1

# Stage 4 — Orchestration (airflow/dags/university_pipeline_dag.py)
# Does NOT run on native Windows (confirmed: os.register_at_fork and an
# airflow.sdk internal import both fail there) — install and run this in
# Google Colab/WSL2/Docker using Airflow's official constraints-pinned
# method instead of a plain `pip install apache-airflow==3.3.0` here.
# See README "Stage 4 — Orchestration" for the exact command.

# Stage 5 — Quality gate (Great Expectations) + Lineage (OpenLineage)
great-expectations==1.19.0
openlineage-python==1.51.0
pandas==3.0.3
